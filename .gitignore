# Keep the public repo to just the website + its data.
# Everything is ignored by default, then we re-allow only what should be public.
*

!.gitignore
!README.md
!export.py
!index.html
!leaderboard.json
!games.json
!stats.json
!players.json
!history.json
!results_log.json
!og-image.png
!config.json
